# Домашнее задание к лекции «Object, Reflection и Proxy»

[![Build status](https://ci.appveyor.com/api/projects/status/w637s9dd2bhwy5pd?svg=true)](https://ci.appveyor.com/project/dm-morozov/netology-44-object-reflection-proxy)
![Netology](https://img.shields.io/badge/Netology-JavaScript-blue)
![Jest Coverage](https://img.shields.io/badge/Покрытие-100%25-brightgreen)

## 📖 Описание проекта

Этот проект выполнен в рамках домашнего задания к лекции [«Object, Reflection и Proxy» курса JavaScript от Нетологии](./README_for_Netology.md). Проект включает реализацию двух задач:

1. **for ... in**: Реализация функции `orderByProps`, которая сортирует свойства объекта по заданному порядку и алфавиту с использованием цикла `for ... in`.
2. **Destructuring**: Реализация метода `getSpecialAttack`, который извлекает данные о специальных атаках персонажа с использованием деструктуризации, добавляя дефолтное описание для засекреченных атак.

Проект построен на основе шаблона Webpack, предоставленного Нетологией, и включает модульную структуру кода, линтер (ESLint), тесты (Jest) с 100% покрытием по строкам, функциям, веткам и операторам, а также интеграцию с CI (AppVeyor).

---

## 🎯 Задачи проекта

### 1. Задача «for ... in»

**Легенда**:  
В рамках разработки игры нужно отображать таблички с информацией об объектах (например, персонажах). Для этого требуется функция, которая возвращает массив свойств объекта со значениями, отсортированный по заданному порядку и алфавиту.

**Описание**:  
Реализована функция `orderByProps` в классе `Character` (файл `src/js/Character.js`). Функция принимает объект и массив ключей для сортировки, возвращая массив объектов вида `{ key, value }`. Порядок сортировки:
- Сначала свойства, указанные в массиве порядка.
- Затем остальные свойства в алфавитном порядке.

**Пример**:  
Входной объект:
```javascript
const character = {
  name: 'мечник',
  health: 10,
  level: 2,
  attack: 80,
  defence: 40
};
```
Порядок сортировки: `["name", "level"]`.  
Вызов: `character.orderByProps(["name", "level"])`.  
Результат:
```javascript
[
  { key: 'name', value: 'мечник' },
  { key: 'level', value: 2 },
  { key: 'attack', value: 80 },
  { key: 'defence', value: 40 },
  { key: 'health', value: 10 }
]
```

**Особенности реализации**:
- Использован цикл `for ... in` для перебора перечисляемых свойств объекта.
- Учтены случаи с несуществующими ключами в массиве порядка.
- Игнорируются неперечисляемые свойства (протестировано с `Object.defineProperty`).
- Тесты в `src/js/__tests__/orderByProps.test.js` покрывают все сценарии: сортировку по порядку, алфавитную сортировку, пустой массив порядка, несуществующие свойства и неперечисляемые свойства.

---

### 2. Задача «Destructuring»

**Легенда**:  
В игре необходимо отображать панель выбора специальных атак персонажа, показывая их `id`, `name`, `icon` и `description`. Для некоторых атак описание засекречено (отсутствует).

**Описание**:  
Реализован метод `getSpecialAttack` в классе `Character` (файл `src/js/Character.js`). Метод использует деструктуризацию для извлечения полей `id`, `name`, `icon`, `description` из массива атак (`special`). Если `description` отсутствует, устанавливается дефолтное значение `'Описание недоступно'`.

**Пример**:  
Входной объект:
```javascript
const character = {
  name: 'Лучник',
  type: 'Bowman',
  health: 50,
  level: 3,
  attack: 40,
  defence: 10,
  special: [
    {
      id: 8,
      name: 'Двойной выстрел',
      icon: 'http://...',
      description: 'Двойной выстрел наносит двойной урон'
    },
    {
      id: 9,
      name: 'Нокаутирующий удар',
      icon: 'http://...'
    }
  ]
};
```
Вызов: `character.getSpecialAttack()`.  
Результат:
```javascript
[
  {
    id: 8,
    name: 'Двойной выстрел',
    icon: 'http://...',
    description: 'Двойной выстрел наносит двойной урон'
  },
  {
    id: 9,
    name: 'Нокаутирующий удар',
    icon: 'http://...',
    description: 'Описание недоступно'
  }
]
```

**Особенности реализации**:
- Использована деструктуризация в `map` для извлечения полей и задания дефолтного значения `description`.
- Обрабатываются случаи: пустой массив `special`, отсутствие `special` (`undefined`), частичные данные атак.
- Тесты в `src/js/__tests__/getSpecialAttack.test.js` покрывают все сценарии: атаки с описанием и без, пустой массив `special`, отсутствие `special`.

---

## 📂 Структура проекта

Проект имеет следующую структуру:

```
Netology_44_object_reflection_proxy/
├── .husky/                     # Настройки Husky для pre-commit хуков
├── .vscode/                    # Настройки VSCode
├── coverage/                   # Отчёты покрытия тестов
├── node_modules/               # Зависимости Node.js
├── src/                        # Исходный код
│   ├── css/                    # Стили (не используются в этом проекте)
│   ├── js/                     # JavaScript код
│   │   ├── __tests__/          # Unit-тесты
│   │   │   ├── Character.test.js         # Тесты для класса Character
│   │   │   ├── getSpecialAttack.test.js  # Тесты для метода getSpecialAttack
│   │   │   └── orderByProps.test.js      # Тесты для метода orderByProps
│   │   ├── Bowman.js           # Класс Bowman
│   │   ├── Character.js        # Базовый класс Character
│   │   ├── Daemon.js           # Класс Daemon
│   │   ├── Magician.js         # Класс Magician
│   │   ├── Swordsman.js        # Класс Swordsman
│   │   ├── Undead.js           # Класс Undead
│   │   ├── Zombie.js           # Класс Zombie
│   │   └── characters__old.js  # Устаревший код (для истории)
│   ├── index.html              # HTML-страница
│   └── index.js                # Точка входа
├── svg/                        # Иконки для README
├── .appveyor.yml               # Конфигурация CI (AppVeyor)
├── .gitattributes              # Настройки Git
├── .gitignore                  # Игнорируемые файлы
├── babel.config.json           # Конфигурация Babel
├── description_homework.md     # Описание задания
├── eslint.config.mjs           # Конфигурация ESLint
├── jest.config.js              # Конфигурация Jest
├── package-lock.json           # Фиксация версий зависимостей
├── package.json                # Описание проекта и скрипты
├── README_for_Netology.md      # Дополнительный README
├── README.md                   # Основной README
├── webpack.common.mjs          # Общая конфигурация Webpack
├── webpack.config.mjs          # Основная конфигурация Webpack
├── webpack.dev.mjs             # Конфигурация для разработки
└── webpack.prod.mjs            # Конфигурация для продакшена
```

---

## 🛠 Технологии и инструменты

- **JavaScript (ES6+)**: Основной язык программирования.
- **Webpack**: Сборщик модулей для организации кода.
- **Babel**: Транспиляция современного JavaScript.
- **Jest**: Фреймворк для unit-тестирования с 100% покрытием.
- **ESLint**: Линтер для обеспечения качества кода.
- **Husky**: Pre-commit хуки для линтинга и тестирования.
- **AppVeyor**: Интеграция с CI для автоматической проверки.
- **Node.js**: Среда выполнения.

---

## 🚀 Установка и запуск

### Требования
- **Node.js** (версия 14 или выше)
- **npm** (устанавливается вместе с Node.js)

### Инструкции

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/dm-morozov/Netology_44_object_reflection_proxy.git
   cd Netology_44_object_reflection_proxy
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   ```

3. **Запустите тесты**:
   ```bash
   npm run test
   ```

4. **Проверьте покрытие тестов**:
   ```bash
   npm run test -- --coverage
   ```

5. **Запустите проект в режиме разработки**:
   ```bash
   npm start
   ```
   Откройте `http://localhost:8080` в браузере.

6. **Проверьте код с помощью ESLint**:
   ```bash
   npm run lint
   npm run lint:fix
   ```

7. **Соберите проект для продакшена**:
   ```bash
   npm run build
   ```

---

## 🧪 Тестирование

Тесты написаны с использованием **Jest** и обеспечивают **100% покрытие** по строкам, функциям, веткам и операторам. Тестовые файлы находятся в `src/js/__tests__/`.

- **`Character.test.js`**: Проверяет создание персонажей, валидацию имени и типа, повышение уровня, нанесение урона.
- **`orderByProps.test.js`**: Проверяет сортировку свойств по заданному порядку, алфавитную сортировку, обработку пустого массива порядка, несуществующих и неперечисляемых свойств.
- **`getSpecialAttack.test.js`**: Проверяет извлечение специальных атак с описанием и без, обработку пустого массива `special` и его отсутствие (`undefined`).

**Пример покрытия**:
```
--------------|---------|----------|---------|---------|-------------------
File          | % Stmts | % Branch | % Funcs | % Lines | Uncovered Line #s
--------------|---------|----------|---------|---------|-------------------
All files     |     100 |      100 |     100 |     100 |
 Bowman.js    |     100 |      100 |     100 |     100 |
 Character.js |     100 |      100 |     100 |     100 |
 Daemon.js    |     100 |      100 |     100 |     100 |
 Magician.js  |     100 |      100 |     100 |     100 |
 Swordsman.js |     100 |      100 |     100 |     100 |
 Undead.js    |     100 |      100 |     100 |     100 |
 Zombie.js    |     100 |      100 |     100 |     100 |
--------------|---------|----------|---------|---------|-------------------
```

---

## 📧 Контакты

Если возникнут вопросы, пишите:
- ![LinkedIn](./svg/linkedin-icon.svg) [LinkedIn](https://www.linkedin.com/in/dm-morozov/)
- ![Telegram](./svg/telegram.svg) [Telegram](https://t.me/dem2014)
- ![GitHub](./svg/github-icon.svg) [GitHub](https://github.com/dm-morozov/)

---

## 📚 Итоги и выводы

Этот проект стал отличной практикой работы с объектами, деструктуризацией и перечислением свойств в JavaScript. Основные достижения:
- Реализованы функции `orderByProps` и `getSpecialAttack` с использованием современных возможностей ES6+.
- Написаны исчерпывающие тесты, обеспечивающие 100% покрытие.
- Настроены линтинг, CI и Webpack для профессиональной разработки.
- Код структурирован и документирован для удобства чтения и поддержки.

Проект демонстрирует глубокое понимание работы с объектами, тестирования и организации кода в JavaScript-проектах.